{
  "name": "mpl-playground",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cd mpl-core && npm run build",
    "dev": "cd mpl-web && npm run dev",
    "install-all": "npm install && cd mpl-core && npm install && cd ../mpl-web && npm install"
  }
}
